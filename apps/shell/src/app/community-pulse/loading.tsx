/**
 * Loading state for CommunityPulse routes
 * Shown during route transitions
 */

import { DashboardSkeleton } from './components/EngagementCardSkeleton';

export default function CommunityPulseLoading() {
  return <DashboardSkeleton />;
}
